import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import MoreMenu from '@/view/client/moremenu'

const activities = () => {
  return (
    <MoreMenu />
  )
}

export default activities

const styles = StyleSheet.create({})